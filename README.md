# AI-Book-Volume2-Inside-the-machine
Python code snippets from a book
pip install -U \
  langchain langchain-community langchain-openai langchain-huggingface \
  python-dotenv faiss-cpu transformers sentence-transformers
pip install openai PyPDF2
pip install requests json 
pip install smtplib
